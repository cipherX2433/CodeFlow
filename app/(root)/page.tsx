import { auth, signOut } from "@/auth";


const Home = async () => {

  return(
  <>
     <section className="flex w-full flex-col-reverse justify-between gap-4 sm:flex-row sm:items">
      All Questions
     </section>
  </>
)
};

export default Home;
